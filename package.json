{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "danger": "danger",
    "precommit": "lint-staged",
    "build": "lerna run build",
    "test": "jest --coverage --maxWorkers=6 --env=jsdom",
    "test:debug": "yarn jest --coverage --debug",
    "precommit": "lint-staged",
    "lint": "lerna run lint",
    "danger-local": "NODE_ENV='development' yarn danger"
  },
  "devDependencies": {
    "enzyme": "^3.3.0",
    "enzyme-to-json": "^3.3.1",
    "eslint": "^4.13.1",
    "eslint-plugin-prettier": "^2.4.0",
    "flow-bin": "^0.72.0",
    "flow-typed": "^2.4.0",
    "husky": "^0.14.3",
    "jest": "^22.1.1",
    "lerna": "^2.11.0",
    "lint-staged": "^6.0.0",
    "prettier": "^1.9.2"
  }
}
