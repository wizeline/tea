// @flow

export type IconProps = {
  // SVG
  name: string,
  // shape
  round?: boolean,
  // sizes
  xsmall?: boolean,
  small?: boolean,
  medium?: boolean,
  large?: boolean,
  xlarge?: boolean,
  // colors
  color?: string,
  background?: string,
};
