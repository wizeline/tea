// @flow

export type IconPropTypes = {
  name: string,
  round?: boolean,
  // Size Props
  xsmall?: boolean,
  small?: boolean,
  medium?: boolean,
  large?: boolean,
  xlarge?: boolean,
  // Color Props
  color?: string,
  background?: string,
};
