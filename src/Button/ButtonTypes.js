// @flow

export type ButtonProps = {
  // Color
  destructive?: boolean,
  primary?: boolean,
  secondary?: boolean,
  // Size
  big?: boolean,
  normal?: boolean,
  small?: boolean,
  // Other types
  disabled?: boolean,
  icon?: string,
  text?: boolean,
};
