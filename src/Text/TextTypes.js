// @flow

import * as React from 'react';

export type TextProps = {
  children?: React.Node,
  color?: string,
  h600?: boolean,
  h500?: boolean,
  h400?: boolean,
  h300?: boolean,
  h200?: boolean,
  b150?: boolean,
  b100?: boolean,
  b50?: boolean,
};
