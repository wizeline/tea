// @flow

export type ButtonPropTypes = {
  text?: string,
  iconName?: string,
  disabled?: boolean,
  round?: boolean,
};

export type ButtonStyleProps = {
  children?: React.Node,
  icon?: React.Element,
};
