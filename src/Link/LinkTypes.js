// @flow

import * as React from 'react';

export type LinkProps = {
  children?: React.Node,
  b50?: boolean,
  b100?: boolean,
  b150?: boolean,
  h300?: boolean,
  href?: string,
  theme?: Object,
};
